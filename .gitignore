# Dependencies
node_modules/

# Generated — rebuilt by `npm run build:docs`
css/crnl.css
dist/
storybook-static/

# Font build scratch — `npm run build:fonts` regenerates from tools/font-lab
tools/font-lab/_build/sources/
tools/font-lab/_build/state*.json

# OS
.DS_Store

# Visual regression. Baselines are machine-specific — text rasterises
# differently per OS and browser build — so they are generated locally by the
# first `npm run check:visual`, never shared.
tests/visual/
